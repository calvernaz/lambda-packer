- lambda-packer lambda is missing the architecture and image name
i.e lambda-packer lambda lambda-apigw-authorizer --type docker --layers common

VERY IMPORTANT:
- build a dockerfile from the lambda configuration without need to provide one
